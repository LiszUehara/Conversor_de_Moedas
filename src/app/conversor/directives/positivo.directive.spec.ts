import { PositivoDirective } from './positivo.directive';

describe('PositivoDirective', () => {
  it('should create an instance', () => {
    const directive = new PositivoDirective();
    expect(directive).toBeTruthy();
  });
});
